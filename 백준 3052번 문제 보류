int main() {
	int arr[10] = {0}, n[1000] = {0}, cnt = 0;
	for (int i = 0; i < 10; i++) {
		scanf("%d", &arr[i]);
		n[i] = arr[i] % 42;
		if (n[i] >= 0) {
			cnt++;
		}
	}
	for (int j = 0; j < 10; j++) {
		if (n[i] == n[j]) {
			cnt--;
		}
	}
	if (cnt < 0) {
		cnt = 1;
	}
	printf("%d", cnt);
	return 0;
}
