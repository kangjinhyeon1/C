#include <stdio.h>

int main() {
	int a[31] = {0}; 
    int cnt;
	for (int i = 0; i < 28; i++) {
		scanf("%d", &cnt);
		a[cnt] = cnt;
	}
	for (int j = 1; j < 31; j++) {
		if (a[j] == 0) {
			printf("%d\n", j);
		}
	}

	return 0;
}
