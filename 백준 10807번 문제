#include <stdio.h>

int main() {
	int a, b[100], c, cnt = 0;
	scanf("%d", &a);
	for (int i = 0; i < a; i++) {
		scanf("%d", &b[i]);
	}
	scanf("%d", &c);
	for (int j = 0; j < a; j++) {
		if (c == b[j]) {
			cnt++;
		}
	}
	printf("%d", cnt);
}
